{
  "name": "test-bootstrap",
  "version": "1.0.0",
  "description": "test project bootstrap",
  "scripts": {
    "build-gh": "parcel build --public-url https://krzysiekm266.github.io/test-bootstrap/ ./src/index.html --dist-dir build/test-bootstrap",
    "build": "parcel build --public-url ./ ./src/index.html --dist-dir build",

    "start": "parcel serve src/index.html --public-url / --dist-dir dist"
  },
  "keywords": [
    "test",
    "bootstrap"
  ],
  "author": "krzysiek",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "sass": "^1.54.9"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.1",
    "bootstrap-icons": "^1.9.1",
    "gh-pages": "^4.0.0"
  }
}
